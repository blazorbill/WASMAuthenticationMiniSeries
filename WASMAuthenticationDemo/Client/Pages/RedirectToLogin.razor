@inject NavigationManager Navigation
@code {
    protected override void OnInitialized()
    {
        string returnUri = Navigation.Uri;
        Navigation.NavigateTo($"login?returnUrl={returnUri}");
    }
}